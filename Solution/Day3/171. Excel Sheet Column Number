Description:
Given a column title as appear in an Excel sheet, return its corresponding column number.

For example:

    A -> 1
    B -> 2
    C -> 3
    ...
    Z -> 26
    AA -> 27
    AB -> 28 
    
    Solution: 此题比较简单，只需要发现这相当于一个base是27的关系，第1个字符对应的数值为26*0+该字符在字母表的位置，第2字符对应的数值为26*1+该字符在字母表的位置。因此
    Solution: 此题比较简单，只需要发现这相当于一个base是27的关系，第1个字符对应的数值为26*0+该字符在字母表的位置，第2字符对应的数值为26*1+该字符在字母表的位置，.
